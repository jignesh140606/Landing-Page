function handleAction(action) {
    if (action === 'readMore') {
        alert("Redirecting to detailed information...");
    } else if (action === 'consult') {
        alert("Redirecting to consultation form...");
    }
}
